#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

void main()
{
    // // Begin 1
    // int p, a = 0;
    // scanf("%i", &a);
    // p = a * 4;
    // printf("Begin 1: P = %i\n", p);

    // // Begin 2
    // int s, a = 0;
    // scanf("%i", &a);
    // s = a * a;
    // printf("Begin 2: S = %i", s);

    // // Begin 3
    // int a = 0, b = 0, s = 0, p = 0;
    // scanf("%i", &a);
    // scanf("%i", &b);
    // s = a * b;
    // p = 2 * (a + b);
    // printf("Begin 3: S = %i, P = %i", s, p);

    // // Begin 4
    // int d = 0, l = 0;
    // const float pi = 3.14f;
    // scanf("%i", &d);
    // l = pi * d;
    // printf("Begin 4: L = %i", l);
    
    // Begin 5
    int a = 0, v = 0;
}