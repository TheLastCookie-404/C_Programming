#include <math.h>
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

void main()
{
    // Begin 1
    // int p, a = 0;
    // scanf("%i", &a);
    // p = a * 4;
    // printf("Begin 1: P = %i\n", p);

    // Begin 2
    // int s, a = 0;
    // scanf("%i", &a);
    // s = a * a;
    // printf("Begin 2: S = %i \n", s);

    // Begin 3
    // int a = 0, b = 0, s = 0, p = 0;
    // scanf("%i", &a);
    // scanf("%i", &b);
    // s = a * b;
    // p = 2 * (a + b);
    // printf("Begin 3: S = %i, P = %i \n", s, p);

    // Begin 4
    // int d = 0, l = 0;
    // const float pi = 3.14f;
    // scanf("%i", &d);
    // l = pi * d;
    // printf("Begin 4: L = %i \n", l);
    
    // Begin 5
    // int a = 0, v = 0, s = 0;
    // scanf("%i", &a);
    // v = a * a * a;
    // s = 6 * a * a;
    // printf("Begin 5: V = %i, S = %i", v, s);

    // Begin 6
    // int a = 0, b = 0, c = 0, v = 0, s = 0;
    // scanf("%i", &a);
    // scanf("%i", &b);
    // scanf("%i", &c);
    // v = a * b * c;
    // s = 2 * (a * b + b * c + a * c);
    // printf("Begin 6: V = %i, S = %i", v, s);

    // Begin 7
    // int l = 0, s = 0, r = 0;
    // const float pi = 3.14f;
    // scanf("%i", &r);
    // l = 2 * pi * r;
    // s = pi * (r * r);
    // printf("Begin 7: L = %i S = %i", l, s);

    // Begin 8
    // int a = 0, b = 0, arithmeticMean = 0;
    // scanf("%i", &a);
    // scanf("%i", &b);
    // arithmeticMean = (a + b) / 2;
    // printf("Begin 8: Arithmetical Middle = %i \n", arithmeticMean);

    // Begin 9
    // unsigned float a = 0, b = 0;
    // scanf("%f", &a);
    // scanf("%f", &b);
    // printf("Begin 9: Geometrical Middle = %lf \n", sqrtf(a * b));

    // Begin 10
    int a = 0, b = 0;
    scanf("%f", &a);
    scanf("%f", &b);
    

}